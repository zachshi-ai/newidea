# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Local decision ledgers (do not commit personal ledgers by default;
# the examples/sample-ledger.json is committed explicitly as a demo).
.decision-debt/

# bus-factor demo repo is rebuilt on demand by examples/build_examples.py
# (it carries git history that does not belong in the parent repo).
bus-factor/examples/demo-repo/

# OS / editor
.DS_Store
.idea/
.vscode/

# churn-hotspot demo repo is rebuilt on demand by examples/build_examples.py
churn-hotspot/examples/demo-repo/
