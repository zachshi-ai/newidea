# 方法论 · The Decision Debt Methodology

`decision-debt` 不只是一个脚本，它是一种**对待决策的纪律**。本文讲清这套纪律背后的为什么，以及怎么把它落成每周 15 分钟的习惯。

---

## 1. 为什么「决策」需要专属的工具

主流工具覆盖了三种工作物件：

| 物件 | 完成态 | 时间锚 | 典型工具 |
|---|---|---|---|
| 任务（Task） | 有（done） | 截止日 | Todoist / Jira / 飞书任务 |
| 日程（Event） | 无（发生即过） | 开始时间 | 日历 |
| **决策（Decision）** | **有，但是「拍板」而不是「做完」** | **无** | **——空缺——** |

决策的尴尬在于：它**太小**进不了任务清单（没有可执行的「完成动作」），**太不确定**写不进文档（结论还没出来），**太重要**又不能忘。于是它只活在脑子里——而大脑是极差的「待定事项队列」：它会反复提醒你（焦虑）、又会随机遗忘（变危机）。

`decision-debt` 的主张：**给决策一个和任务、日程并列的第三种容器**，并用「债务」这个每个人都懂的隐喻，让它的隐性成本变得可见、可排序、可结清。

## 2. 债务隐喻的四个成分

```
DEBT = 权重 × 年龄 × 基础利率 × 陈旧度
        ─────   ────   ────────   ──────
        本金    时间    反悔惩罚   忽视惩罚
```

每一项都对应一种真实的认知失误：

- **权重 × 年龄（本金 × 时间）**：越大、越久没定的决策，认知占用越多。这是「本金 + 时间价值」。
- **基础利率 = 1 + 0.5 × 重开次数（反悔惩罚）**：一个被反复「定了又推翻」的决策，在告诉你**信息不足或判断不稳**——它是高风险决策，理应更贵、更该被优先处理或拆解。
- **陈旧度 = 1 + min((今天−上次审视)/14, 5)（忽视惩罚）**：开启后就再也不看的决策最危险——你既没结清、也没在主动管理，它在后台默默复利。每 14 天不审视，债务翻倍（封顶 6×）。
- **关闭即归零**：`commit` 或 `abandon` 让它从「债」变成「史」，停止计息，进入决策日志。

> 设计要点：这套公式**不是科学测量**，而是一个**相对排序信号**。它的唯一目的是让你在每天/每周问出同一个问题——「此刻我最该先定哪几件事？」——并给出稳定可比较的答案。

## 3. 每周还债仪式（15 分钟）

挑一个固定时段（建议周五下午收尾，或周一开工）。固定时间比时长重要——仪式靠重复存活。

**第 1 步 · 看账（2 分钟）**
```bash
decision-debt report
decision-debt review --top 5
```

**第 2 步 · 还债（10 分钟，对最烫的每笔三选一）**

| 你能做的 | 命令 | 含义 |
|---|---|---|
| 现在就能定 | `commit <id> --outcome "选 X，因为 Y"` | 拍板，停息，留痕 |
| 不该你定 / 已无意义 | `abandon <id> --reason "由设计师定" / "需求已撤销"` | 止损结清，别再占内存 |
| 还缺关键信息 | 把缺的信息写进 `context`，再 `touch <id>` | 承认你在管它，重置陈旧度 |

**第 3 步 · 对账（3 分钟）**
```bash
decision-debt report
```
看总债务趋势。**目标是趋势向下，不是清零。** 永远有开放决策是健康的；让它们处于「被审视、有期限感」的状态，才是仪式的目的。

## 4. 反模式（别这么用）

- ❌ **把决策当任务**：「研究定价模式」是任务，「选哪种定价模式」才是决策。任务有动作，决策有结论。混在一起会让债务分失真。
- ❌ **只 add 不结清**：账本只会越积越贵。仪式的关键是「三选一」，不是「记录」。
- ❌ **用 `touch` 拖延**：`touch` 是「我在主动管理、但还缺信息」，不是「我不想定」。如果一笔决策被反复 touch 却从不 commit/abandon，说明它该被**拆小**或**显式 abandon**。
- ❌ **追求债务为 0**：会逼你对还没想清的事过早拍板，制造更糟的决策。保留健康的开放债，只要它在被审视。

## 5. 进阶用法

- **多账本**：每个项目/角色一个目录，各自 `init`；或用 `--ledger` / `DECISION_LEDGER` 切换。
- **决策日志做复盘**：定期 `export`，按月回看「这个月我拍板了哪些、放弃了哪些、平均挂了几天」，能看出自己**在哪类决策上拖延最久**。
- **版本化**：把 `.decision-debt/ledger.json` 提交进 git，决策历史就和代码一样可 diff、可追溯。
- **接入 AI 工作流**：决策日志是结构化的 markdown，可直接喂给 Agent 做「本月决策复盘」「找出我反复重开的决策」等分析。

## 6. FAQ

**Q：债务分具体多少算高？**
A：没有绝对阈值，只看相对排序和自身趋势。新账本里 50 分可能就是榜首；成熟账本里 500 分也正常。盯**榜首几笔**和**总债务趋势**，别盯绝对值。

**Q：权重怎么定？**
A：1 = 可逆/低影响（配色、命名），3 = 常规（默认值，多数决策），5 = 难逆转/高代价（定价、技术栈、关键招聘）。拿不准就给 3。

**Q：公式能调吗？**
A：能。`decision_debt.py` 顶部四个常量（`REOPEN_RATE` / `REVIEW_HORIZON_DAYS` / `STALENESS_CAP` / `DEFAULT_WEIGHT`）就是给你调的。比如觉得 14 天审视周期太短，改成 21，陈旧度增长会更缓。改完跑测试确认没破坏行为即可。

**Q：为什么不做个 App / 飞书集成？**
A：CLI + JSON 的形态是**有意为之的极简**——它要像 `git` 一样轻、快、零摩擦，让你 5 秒钟就能记一笔。形态越重，记录摩擦越大，仪式越容易死。集成可以在此基础上加，但核心必须轻。
